# Physical-layer simulation dependencies (subset of root requirements.txt).
# Prefer installing from the repo root:
#   pip install -r requirements.txt          ← installs everything
#
# Or install only simulation deps:
#   pip install numpy scipy matplotlib streamlit skyfield

numpy>=1.26.0
scipy>=1.13.0
matplotlib>=3.9.0
streamlit>=1.35.0
skyfield>=1.48
